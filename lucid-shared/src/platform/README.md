# Platform

`PlatformSimObject<T>` is the contract between a Lucid shape wrapper and the
domain object it carries; the extension's `SimObjectLucid` implements it.

`PlatformType` / `PlatformMetadata` come from
`@quodsi/shared` (`quodsi_shared/src/platform`) and are re-exported here.
