/**
 * Enum of available validation rule names for type-safe rule logging configuration.
 */
export enum ValidationRuleName {
    ElementCounts = 'ElementCountsValidation',
    Activity = 'ActivityValidation',
    Connector = 'ConnectorValidation',
    Generator = 'GeneratorValidation',
    Resource = 'ResourceValidation',
    Entity = 'EntityValidation'
}
